apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "npd-helm-test.fullname" . }}-test-connection"
  labels:
    {{- include "npd-helm-test.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "npd-helm-test.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
