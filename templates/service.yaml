apiVersion: v1
kind: Service
metadata:
  name: {{ include "npd-helm-test.fullname" . }}
  labels:
    {{- include "npd-helm-test.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "npd-helm-test.selectorLabels" . | nindent 4 }}
